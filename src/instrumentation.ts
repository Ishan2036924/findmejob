// One-time app boot. Runs once when the Node.js runtime starts.
// AI Gateway is the default provider for AI SDK string model IDs — no override needed.
// Future hooks: OpenTelemetry init, Sentry init, prompt-cache warmup.

export async function register() {
  // Intentionally empty for now.
}
